<salla-menu class="dar-main-menu">
  <nav class="flex flex-col md:flex-row items-start md:items-center gap-1 md:gap-0 py-1">
    <ul class="flex flex-col md:flex-row items-start md:items-center w-full md:w-auto">
      {# Menu items are auto-populated by salla-menu component #}
    </ul>
  </nav>
</salla-menu>

<style>
  .dar-main-menu a {
    display: block;
    padding: 0.5rem 1rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--dar-dark);
    transition: color 0.2s;
  }
  .dar-main-menu a:hover {
    color: var(--dar-olive);
  }
</style>
