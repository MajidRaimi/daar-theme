{% extends "layouts.master" %}

{% block title %}{{ trans('pages.notifications.title') }} - {{ store.name }}{% endblock %}

{% block content %}
  {% component 'header.breadcrumbs' %}

  <div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-dar-dark mb-8">{{ trans('pages.notifications.title') }}</h1>
    <salla-notifications-list class="dar-notifications"></salla-notifications-list>
  </div>
{% endblock %}
