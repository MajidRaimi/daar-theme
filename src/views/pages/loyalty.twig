{% extends "layouts.master" %}

{% block title %}{{ trans('pages.loyalty.title') }} - {{ store.name }}{% endblock %}

{% block content %}
  {% component 'header.breadcrumbs' %}

  <div class="container mx-auto px-4 py-8">
    <h1 class="text-2xl md:text-3xl font-bold text-dar-dark mb-8">{{ trans('pages.loyalty.title') }}</h1>
    <salla-loyalty-program class="dar-loyalty"></salla-loyalty-program>
  </div>
{% endblock %}
